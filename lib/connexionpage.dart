// lib/connexionpage.dart

import 'package:flutter/material.dart';

class ConnexionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page '),
      ),
      body: Center(
        child: Text('Bienvenue à la page de connexion!'),
      ),
    );
  }
}
