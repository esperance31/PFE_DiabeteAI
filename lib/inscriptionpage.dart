// lib/inscriptionpage.dart

import 'package:flutter/material.dart';

class InscriptionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Troisième Page'),
      ),
      body: Center(
        child: Text('Bienvenue à la troisième page!'),
      ),
    );
  }
}
